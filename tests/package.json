{
  "name": "domus-tests",
  "private": true,
  "description": "DOM-level tests for Domus — runs index.html in jsdom under node's built-in test runner",
  "scripts": { "test": "node --test" },
  "devDependencies": { "jsdom": "24.1.3" }
}
